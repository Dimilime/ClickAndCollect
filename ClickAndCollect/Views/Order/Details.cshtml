@model Order
@*
    @if (Model != null)
    {
        <h1>Détails de la commande n°: @Model.OrderID</h1>
        <table class="table">
            <tr>
                <th>Nom du Produit : </th>
                <td>@Model.</td>
            </tr>
            <tr>
                <th>Prix (€) : </th>
                <td>@Model.Price</td>
            </tr>
            <tr>
                <th>Type de produit : </th>
                <td>@Model.Type</td>
            </tr>
        </table>
    }
    else
    {
        <h1>Erreur</h1>
        <p>@ViewData["Error"]</p>
    }*@
<a asp-action="Index">Voir la liste des produits</a>